import clientPromise from "../../lib/mongodb";

export default async function addSearchedWebsites(arr, website, zone){
    try{

    }catch(err){
        if(err){
            console.error(err);
        }
    }
}