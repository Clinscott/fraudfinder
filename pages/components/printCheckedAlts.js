//  import {testData} from "./data.js"

export default function printCheckedAlts(arr){
    const failedAlts = arr.join(", ")
    return failedAlts
}

//  console.log(printCheckedAlts(testData.unRegisteredAlts))
